数据处理节点：
  分为过程节点 和 数据处理节点  先简单分析 拿到入参 （入参为挑选 所以入参数据类型与结构已经确定了）
  最终返回结果也确定了 但是中间的梳理过程未确定

  每一个数据处理节点组件 都相当于一个函数 接受入参 和 返回
  例子 数组处理节点 选择方法->剔除|筛选

  组件入参 对于复杂结构类型的 目前已定义 简单对象 简单数组 对象数组 二维对象数组 树


  1  数据处理 分析
 /* 逻辑类： if 判断、switch map对应关系、循环

  对象： 增加键值 删除键值 遍历重组 挑选键值

  数组：
   （1） 修改
   （2） 重组
   （3） 长度*/ (弃用）

   方案二：
   采用关键字 来处理

   switch Map 转换
   对象： 增加键值 删除键值 取键值 转数组
   数组： 遍历修改 遍历重组 过滤 查询
